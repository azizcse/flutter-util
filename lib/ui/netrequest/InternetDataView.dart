import 'package:flutter/material.dart';

class InternetDataView extends StatelessWidget {
  const InternetDataView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
